<UserControl x:Class="HotelProject.View.DbManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HotelProject.View"
             xmlns:vm="clr-namespace:HotelProject.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
 
    </UserControl.Resources>
    <Grid>
        <StackPanel>
            <Button Content="Build Tables"
                    Command="{Binding CreateTable}"/>
            <Button Content="Delete All Tables"
                    Command="{Binding DeleteAllTables}"/>
        </StackPanel>
    </Grid>
</UserControl>
